{
  "name": "wordle-friends-api",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "check-types": "tsc --noEmit",
    "prebuild": "rm -rf dist && npm install --no-workspaces --production --no-package-lock",
    "build": "tsc",
    "postbuild": "rm -f dist/*.test.js dist/**/*.test.js api.zip && zip -r ./api.zip node_modules dist",
    "format": "prettier --write .",
    "lint": "eslint --ext ts src",
    "lint:fix": "npm run lint -- --fix",
    "test": "jest",
    "watch": "tsc -w"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.54.0",
    "@aws-sdk/client-dynamodb": "^3.54.0",
    "@aws-sdk/lib-dynamodb": "^3.54.0",
    "dayjs": "^1.10.8",
    "nanoid": "^3.3.1",
    "wordle-friends-graphql": "file:../graphql"
  },
  "devDependencies": {
    "@aws-sdk/types": "^3.54.0",
    "@types/aws-lambda": "^8.10.93",
    "@types/jest": "^27.4.1",
    "@typescript-eslint/eslint-plugin": "^5.14.0",
    "aws-lambda": "^1.0.7",
    "aws-sdk-client-mock": "^0.6.2",
    "eslint": "^8.11.0",
    "eslint-config-wordle-friends": "file:../eslint-config",
    "eslint-plugin-import": "^2.25.4",
    "jest": "^27.5.1",
    "prettier": "^2.5.1",
    "ts-jest": "^27.1.3"
  }
}
