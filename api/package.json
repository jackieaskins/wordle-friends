{
  "name": "wordle-friends-api",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "check-types": "tsc --noEmit",
    "prebuild": "npm install --no-workspaces --production --no-package-lock",
    "build": "tsc",
    "postbuild": "rm -f api.zip && zip -r ./api.zip node_modules dist",
    "format": "prettier --write .",
    "lint": "eslint --ext ts src",
    "lint:fix": "npm run lint -- --fix",
    "watch": "tsc -w"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.54.0",
    "@aws-sdk/client-dynamodb": "^3.54.0",
    "@aws-sdk/lib-dynamodb": "^3.54.0",
    "dayjs": "^1.10.8",
    "nanoid": "^3.3.1",
    "wordle-friends-graphql": "file:../graphql"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.93",
    "@typescript-eslint/eslint-plugin": "^5.14.0",
    "aws-lambda": "^1.0.7",
    "eslint": "^8.11.0",
    "eslint-config-wordle-friends": "file:../eslint-config",
    "eslint-plugin-import": "^2.25.4",
    "prettier": "^2.5.1"
  }
}
